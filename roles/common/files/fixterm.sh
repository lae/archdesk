#!/bin/bash
# Managed by Ansible, modify it in the repo!
if [[ "x$TERM" == "xscreen.rxvt" ]]; then export TERM="rxvt-256color"; fi
if [[ "x$TERM" == "xscreen-256color-bce" ]]; then export TERM="screen-256color"; fi
